{{> head }}
	{{> header }}

  <main class="container policy">
    <h1>{{title}}</h1>

    <p class="policy-date"><em>Updated: {{formatDay updated}}</em></p>
	{{{ contents }}}
  </main>

  {{> footer }}
	{{> scripts }}
{{> foot }}
